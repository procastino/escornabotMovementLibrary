#include <Escornabot.h>

Escornabot centola;


void setup(){
  //isto pode ir para a libreria
  
}

void loop()
{
  centola.siBotonIr();
  centola.andaCm(12);
  centola.xiraGraos(72);
  centola.andaCm(12);
  centola.xiraGraos(72);
  centola.andaCm(12);
  centola.xiraGraos(72);
  centola.andaCm(12);
  centola.xiraGraos(72);
  centola.andaCm(12);
  centola.xiraGraos(72);
  centola.pita(200);
  }
